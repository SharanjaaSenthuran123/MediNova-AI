/** Shown when fetch fails — usually the API server (port 4000) is not running. */
export const API_OFFLINE_MESSAGE =
  "Cannot reach the MediNova API. From the project root run npm run dev (starts API + web). Ensure MongoDB is running and run npm run seed for demo accounts.";

export const API_OFFLINE_TOAST =
  "API server offline — run npm run dev from the project root.";
