# Vercel environment variables (Settings → Environment Variables)

# Required for login on Vercel (MongoDB direct auth)
MONGODB_URI=mongodb+srv://USER:PASS@CLUSTER.mongodb.net/medinova
JWT_SECRET=your-long-random-secret
CLIENT_URL=https://medi-nova-ai-2fqi.vercel.app

# Optional — full backend (pharmacy, orders, sockets, admin APIs)
# Deploy server/ to Koyeb/Render/Railway first, then set:
# API_URL=https://your-api.koyeb.app
# NEXT_PUBLIC_SOCKET_URL=https://your-api.koyeb.app

# Optional — live AI features in Next.js routes
# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=...
