# Copy to .env.local for live AI Symptom Checker (optional — demo mode works without it)
OPENAI_API_KEY=your_openai_api_key_here
# Optional model override (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
